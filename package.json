{
  "name": "diff-keypath",
  "version": "0.0.0",
  "description": "A diff algorithm based on the unique key path of each value in an Object / JSON",
  "main": "lib/index.js",
  "repository": "git@github.com:yeojz/diff-keypath.git",
  "author": "Gerald Yeo <contact@fusedthought.com>",
  "license": "MIT",
  "scripts": {
    "build": "rollup -c",
    "clean": "rimraf lib coverage",
    "coverage": "jest --coverage",
    "coveralls": "cat ./coverage/lcov.info | coveralls",
    "lint": "eslint --ext js src tests",
    "test": "jest"
  },
  "keywords": [
    "diff",
    "json",
    "key",
    "path",
    "changeset",
    "object"
  ],
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "coveralls": "^2.13.1",
    "eslint": "^4.0.0",
    "jest": "^20.0.4",
    "rimraf": "^2.6.1",
    "rollup": "^0.43.0"
  },
  "dependencies": {
    "flat": "^2.0.1"
  }
}
